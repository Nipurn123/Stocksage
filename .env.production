# Environment variables for production (Vercel)
# These are publicly visible in the repo, so don't put any actual secrets here
# Use Vercel's environment variables UI instead for real secrets

# Make sure these basic settings are available during build
NEXTAUTH_SECRET=stocksage-next-auth-secret-key-2025-production
NEXTAUTH_URL=https://stocksage-osbt6m1ry-nipurns-projects-738e757b.vercel.app

# Database placeholder (will be overridden by Vercel environment variable)
DATABASE_URL="file:./prisma/dev.db"

# Gemini API Key (will be overridden by Vercel environment variable)
GEMINI_API_KEY="placeholder-replace-in-vercel-dashboard" 